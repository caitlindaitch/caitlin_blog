<div class="header-detail">
  <h1>Edit Comment</h2>
</div>

<%= form_for [@post, @comment] do |f| %>
  <%= f.label :content %>
  <%= f.text_field :content %>
  <%= f.hidden_field :post_id %>
  <%= f.submit %>
<% end %>
