<div class="header">
  <h1>Caitlin Daitch</h1>
  <h2>"I don't know where I'm going from here, but I promise it won't be boring." - David Bowie</h2>
</div>
<div class="container">
  <div class="button">
    <span class="inner-button"><%= link_to "New Post", new_post_url %></span>
  </div>

  <% @posts.each do |post| %>
    <h2><%= link_to post.title, post %></h2>
    <span class="byline">By <%= post.author %> at <%= post.created_at %></span>
    <div class="post-home"><%= post.content %></div>
  <% end %>
</div>
